name: 'payment_gateway'
version: '1.0.0'
config-version: 2

profile: 'payment_gateway'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  payment_gateway:
